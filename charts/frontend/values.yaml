# Default values for frontend chart.

replicaCount: 1

image:
  repository: ghcr.io/railgorail/frontend
  pullPolicy: IfNotPresent
  # Overridden by CI
  tag: v0.0.0

service:
  type: ClusterIP
  port: 80
  targetPort: 8080
