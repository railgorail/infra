# Default values for backend chart.

replicaCount: 1

image:
  repository: ghcr.io/railgorail/backend
  pullPolicy: IfNotPresent
  # Overridden by CI
  tag: v0.0.3

service:
  type: ClusterIP
  port: 80
  targetPort: 8080